using Xunit;

namespace RumbleRaffle.Api.UnitTests;

// Placeholder for the CI walking-skeleton milestone — proves the unit-test
// pipeline step runs and gates the pipeline, before any real unit tests exist.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_AlwaysPasses()
    {
        Assert.True(true);
    }
}
