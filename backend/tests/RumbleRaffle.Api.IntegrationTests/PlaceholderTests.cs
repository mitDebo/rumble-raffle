using Xunit;

namespace RumbleRaffle.Api.IntegrationTests;

// Placeholder for the CI walking-skeleton milestone — proves the
// integration-test pipeline step runs and gates the pipeline, before any
// real integration tests (e.g. WebApplicationFactory-based) exist.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_AlwaysPasses()
    {
        Assert.True(true);
    }
}
